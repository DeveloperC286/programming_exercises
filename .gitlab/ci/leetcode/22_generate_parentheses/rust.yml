22_generate_parentheses-rust-formatting:
  stage: formatting
  image: rust
  before_script:
    - rustup component add rustfmt
  script:
    - cd leetcode/22_generate_parentheses/rust
    - cargo fmt --all -- --check
  rules:
    - if: $CI_MERGE_REQUEST_ID


22_generate_parentheses-rust-linting:
  stage: linting
  image: rust
  before_script:
    - rustup component add clippy
  script:
    - cd leetcode/22_generate_parentheses/rust
    - cargo clippy --verbose --workspace --all-targets -- -D warnings
    - cargo clippy --verbose --workspace --all-targets --all-features -- -D warnings
    - cargo clippy --verbose --workspace --all-targets --no-default-features -- -D warnings
  rules:
    - if: $CI_MERGE_REQUEST_ID


22_generate_parentheses-rust-compiling:
  stage: compiling
  image: rust
  script:
    - cd leetcode/22_generate_parentheses/rust
    - cargo build --verbose --workspace
    - cargo build --verbose --workspace --all-features
    - cargo build --verbose --workspace --no-default-features
  rules:
    - if: $CI_MERGE_REQUEST_ID
    - if: $CI_COMMIT_BRANCH == "master"


22_generate_parentheses-rust-unit-testing:
  stage: unit-testing
  image: rust
  script:
    - cd leetcode/22_generate_parentheses/rust
    - cargo test --verbose --workspace
    - cargo test --verbose --workspace --all-features
    - cargo test --verbose --workspace --no-default-features
  rules:
    - if: $CI_MERGE_REQUEST_ID
    - if: $CI_COMMIT_BRANCH == "master"
