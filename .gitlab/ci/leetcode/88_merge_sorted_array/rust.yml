88_merge_sorted_array-rust-formatting:
  stage: formatting
  image: rust
  before_script:
    - rustup component add rustfmt
  script:
    - cd leetcode/88_merge_sorted_array/rust
    - cargo fmt --all -- --check
  rules:
    - if: $CI_MERGE_REQUEST_ID


88_merge_sorted_array-rust-linting:
  stage: linting
  image: rust
  before_script:
    - rustup component add clippy
  script:
    - cd leetcode/88_merge_sorted_array/rust
    - cargo clippy --verbose --workspace --all-targets -- -D warnings
    - cargo clippy --verbose --workspace --all-targets --all-features -- -D warnings
    - cargo clippy --verbose --workspace --all-targets --no-default-features -- -D warnings
  rules:
    - if: $CI_MERGE_REQUEST_ID


88_merge_sorted_array-rust-compiling:
  stage: compiling
  image: rust
  script:
    - cd leetcode/88_merge_sorted_array/rust
    - cargo build --verbose --workspace
    - cargo build --verbose --workspace --all-features
    - cargo build --verbose --workspace --no-default-features
  rules:
    - if: $CI_MERGE_REQUEST_ID
    - if: $CI_COMMIT_BRANCH == "master"


88_merge_sorted_array-rust-unit-testing:
  stage: unit-testing
  image: rust
  script:
    - cd leetcode/88_merge_sorted_array/rust
    - cargo test --verbose --workspace
    - cargo test --verbose --workspace --all-features
    - cargo test --verbose --workspace --no-default-features
  rules:
    - if: $CI_MERGE_REQUEST_ID
    - if: $CI_COMMIT_BRANCH == "master"
